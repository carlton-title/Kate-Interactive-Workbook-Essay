name: Improve Submission History

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  patch-history-viewer:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Apply submission history viewer
        run: python .github/scripts/patch_submission_history.py
