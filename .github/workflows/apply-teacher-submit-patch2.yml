name: Apply Corrected Teacher Submit Patch
on:
  push:
    branches:
      - main
permissions:
  contents: write
jobs:
  update:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Patch workbook
        run: python scripts/teacher_submit_patch2.py
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f scripts/teacher_submit_patch2.py .github/workflows/apply-teacher-submit-patch2.yml
          git add index.html scripts/teacher_submit_patch2.py .github/workflows/apply-teacher-submit-patch2.yml
          git commit -m "Switch student submissions to teacher review"
          git push
